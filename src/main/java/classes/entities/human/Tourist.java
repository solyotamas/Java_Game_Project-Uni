package classes.entities.human;

import classes.entities.Human;

public class Tourist extends Human {
    public Tourist(int x, int y, int size, String imgURL) {
        super(x, y, size, imgURL);
    }
}
