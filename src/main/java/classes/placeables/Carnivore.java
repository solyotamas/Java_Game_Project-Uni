package classes.placeables;

import java.util.ArrayList;

public class Carnivore extends Animal {
    public Carnivore(int x, int y, int size, String imgURL, ArrayList<Placeable> consumes) {
        super(x, y, size, imgURL, consumes);
    }
}